package baseline;

public class filter {

    public Character [] filterEvenNumbers(Character [] numberArray){
        //create new array with length of input numberArray
        Character [] newNumberArray = new Character[numberArray.length];

        //for each element in input array, if number mod 2 is 0
        //then add it into the new array

        //return new array
    }
}
